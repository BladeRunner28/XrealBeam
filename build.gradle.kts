// Toolchain versions are pinned deliberately: AGP 9.4.0 (latest stable) with the
// Gradle 9.7.1 installed by Homebrew, JDK 17 (JAVA_HOME), compileSdk 36.
plugins {
    id("com.android.application") version "9.4.0" apply false
}
